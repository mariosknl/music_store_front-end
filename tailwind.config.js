module.exports = {
  purge: ['src/**/*.js', 'src/**/*.jsx', 'src/**/*.html'],
  theme: {
    height: {
      xxl: '25rem',
    },
  },
  variants: {},
  plugins: [],
};

// NODE_ENV=Production to run purge and minimize the build command
